---
interface Props {
	title?: string
	body?: string
}

const { title, body } = Astro.props
---

<>
	{ title && <h2 class=" font-bold ">{title}</h2>}
	{ body && <p class=" font-light ">{body}</p>}
	<slot />
</>

